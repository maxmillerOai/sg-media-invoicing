CREATE TABLE SettingEntity (
    key TEXT PRIMARY KEY NOT NULL,
    value TEXT NOT NULL
);

upsert:
INSERT OR REPLACE INTO SettingEntity(key, value) VALUES (?, ?);

get:
SELECT value FROM SettingEntity WHERE key = ?;
